# Copy to .env and adjust.

# Catch-all mailbox domain used for generated account emails.
CATCHALL_DOMAIN=lettermailing.com

# Optional: proxy file path override (default: proxies.txt in the repo root).
# PROXIES_FILE=proxies.txt
